[push]
    default = simple
[pull]
    # Abort the pull if it can't be performed by a fast-forward. Run one of:
    #   git pull --merge
    #   git pull --rebase
    # to explicitly choose how to handle this situation.
    ff = only
[color]
    ui = false
    diff = true
    status = true
[color "decorate"]
    HEAD = blue
    branch = blue
    remoteBranch = blue
    tag = blue
    stash = blue
[color "diff"]
    commit = blue
    frag = blue
[color "status"]
    changed = cyan
    untracked = yellow
[core]
[include]
    path = ./config.local
[diff]
    wsErrorHighlight = all
